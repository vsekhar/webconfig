// This example highlights the simplest possible configuration that usees all
// the defaults. It would be equivalent to not specifying any policy at all.

import "github.com/vsekhar/webconfig"

WebConfig: webconfig.Default
